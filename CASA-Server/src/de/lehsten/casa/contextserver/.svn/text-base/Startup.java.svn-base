/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package de.lehsten.casa.contextserver;

import javax.naming.Context;
import javax.naming.InitialContext;
import javax.naming.NamingException;
import javax.swing.JOptionPane;
import javax.xml.ws.Endpoint;
//import org.apache.activemq.broker.BrokerService;

/**
 *
 * @author phil
 */
public class Startup {
    
   public static final void main(String[] args) {
		
		
		/*	BrokerService broker = new BrokerService();

			// configure the broker
			broker.addConnector("tcp://localhost:61616");

			broker.start();
			
			Context jndiContext = null;
*/
			try {
//	            jndiContext = new InitialContext();
/*	        } catch (NamingException e) {
	            System.out.println("Could not create JNDI API context: " + e.toString());
	            System.exit(1);
	        }
*/			
//	        ContextServer cs = new ContextServer();
	/*		PositionGatherer pg = new PositionGatherer();
			Endpoint eventEndpoint = Endpoint.publish("http://localhost:8090/EventWS?wsdl",
	                new EventWS());
			Endpoint personEndpoint = Endpoint.publish("http://localhost:8090/PersonWS?wsdl",
	                new PersonWS());
			Endpoint locationEndpoint = Endpoint.publish("http://localhost:8090/LocationWS?wsdl",
	                new LocationWS());
	*/		
			JOptionPane.showMessageDialog(null, "Shutdown Server");
	/*		
	        eventEndpoint.stop();
	        personEndpoint.stop();
	        locationEndpoint.stop();
	*/		
	//		GetPersonByUsername("test_autor",ksession);
	//		GetEventsByUsername("test_autor",ksession);
	//		GetEventsByTitle("Forschungsseminar",ksession);
	//		GetLecturesByTitle("Forschungsseminar",ksession);
	//		GetEventsByDate(1244113200L,ksession);
	//		GetEventsByRoom("Hörsaal 1",ksession);
	//		GetEventsByRoomAndByDate("e-Learning Werkstatt", 1256209200L,ksession);
	//		GetNextEventForRoom("e-Learning Werkstatt",ksession);
	 
	 
	//		GetNextEventForUsername("test_autor",cs.ksession);
			
			
			/*
			Object[] myargs = new Object[1];
			myargs[0] = "test_autor";
//			myargs[1] = (new Date().getTime() / 1000);
			QueryResults results = ksession.getQueryResults( "GetPersonByUsername", myargs );
			System.out.println( "we have " + results.size() + " events" );

			System.out.println( "These events are:" );

			for ( QueryResultsRow row : results ) {
			    Person person = ( Person ) row.get( "p" );
			    System.out.print( person.getUsername() + "\n" );
			    
			//    System.out.print( "Von "+new Date(event.getBegin()*1000) + " bis " +new Date(event.getEnd()*1000)+ "\n" );
			    
			//    System.out.println("Super:"+event.getSuperEvent().getTitle());
			}
		*/
			
		//	logger.close();
		//	server.destroy();
	//		broker.stop();
			
		} catch (Throwable t) {
			t.printStackTrace();
		}
	}
    
}
